export interface url {
  originalUrl: string;
  shortUrl: string;
}

// model Url {
//     id          String @id @default(cuid()) @map("_id")
//     originalUrl String
//     shortUrl    String @unique
//   }
